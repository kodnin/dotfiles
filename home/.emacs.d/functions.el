(defun functions-open ()
  (interactive)
  (find-file "~/.emacs.d/functions.el"))

(defun functions-load ()
  (interactive)
  (load "~/.emacs.d/functions.el"))

(defun init-open ()
  (interactive)
  (find-file "~/.emacs.d/init.el"))

(defun init-load ()
  (interactive)
  (load "~/.emacs.d/init.el"))

(defun packages-open ()
  (interactive)
  (find-file "~/.emacs.d/packages.el"))

(defun packages-load ()
  (interactive)
  (load "~/.emacs.d/packages.el"))
